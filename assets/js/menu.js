
(function($) {

// Menu Animated Modals //

  // Row 1 //
	$("#drinksLink").animatedModal({
		modalTarget: "drinksModal"
	});
	$("#appetizerLink").animatedModal({
		modalTarget: "appetizerModal"
	});
	$("#chaatLink").animatedModal({
		modalTarget: "chaatModal"
	});
	$("#kebabsLink").animatedModal({
		modalTarget: "kebabsModal"
	});
  // Row 2 //
  $("#vegeLink").animatedModal({
		modalTarget: "vegeModal"
	});
	$("#chickenLink").animatedModal({
		modalTarget: "chickenModal"
	});
	$("#meatLink").animatedModal({
		modalTarget: "meatModal"
	});
	$("#seafoodLink").animatedModal({
		modalTarget: "seafoodModal"
	});
  // Row 3 //
  $("#riceLink").animatedModal({
		modalTarget: "riceModal"
	});
	$("#tandooriLink").animatedModal({
		modalTarget: "tandooriModal"
	});
	$("#sideLink").animatedModal({
		modalTarget: "sideModal"
	});
	$("#dessertLink").animatedModal({
		modalTarget: "dessertModal"
	});
	
})(jQuery);